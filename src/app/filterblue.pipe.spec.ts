import { FilterbluePipe } from './filterblue.pipe';

describe('FilterbluePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterbluePipe();
    expect(pipe).toBeTruthy();
  });
});
